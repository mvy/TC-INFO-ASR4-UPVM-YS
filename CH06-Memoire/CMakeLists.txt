PROJECT(TC-INFO-ASR3-UPVM-YS-CH06 NONE)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
INCLUDE(../UseLATEX.cmake)

SET(LATEX_OUTPUT_PATH ../build)


ADD_LATEX_DOCUMENT(
    CH06-Slides.tex MANGLE_TARGET_NAMES
    INPUTS CH06-Content.tex
    IMAGE_DIRS images
    DEFAULT_PDF
    )

ADD_LATEX_DOCUMENT(
    CH06-Handout.tex MANGLE_TARGET_NAMES
    INPUTS CH06-Content.tex
    IMAGE_DIRS images
    DEFAULT_PDF
    )

